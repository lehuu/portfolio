import * as React from 'react';
import styled from 'styled-components';
import { Layout } from '../components';

const IndexPage = () => (
  <Layout title="Phuoc Le">
    <h1>Currently under maintenace</h1>
  </Layout>
);

export default IndexPage;
